import { expect } from "chai";

describe("Routes", () => {
  describe("/api/resources", () => {
    it("should return status 200", () => {});
  });
  describe("/api/tags", () => {
    it("should return status 200", () => {});
  });
  describe("/api/areas", () => {
    it("should return status 200", () => {});
  });
});
